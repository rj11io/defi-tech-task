interface:
  display_name: "11ai MongoDB Databases"
  short_description: "Inspect and manage MongoDB databases"
  default_prompt: "Use $11ai-mongodb-databases to inspect, select, create, or deliberately drop a MongoDB database."
